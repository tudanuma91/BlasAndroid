<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.v3.basis.blas">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- TODO: Delete!! this is for test environment -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".blasclass.app.BlasApp"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.FixtureSearchResultActivity"></activity>
        <activity android:name=".activity.ItemSearchResultActivity" />
        <activity android:name=".activity.QRActivity" />
        <activity android:name=".activity.Test3Activity" />
        <activity android:name=".activity.Test2Activity" />
        <activity android:name=".activity.Test1Activity" />
        <activity
            android:name=".activity.ItemImageActivity"
            android:label="@string/title_item_image" />
        <activity
            android:name=".activity.ItemEditActivity"
            android:label="@string/title_item_edit" />
        <activity android:name=".activity.FixtureActivity" />
        <activity android:name=".activity.ItemActivity" />
        <activity android:name=".activity.TerminalActivity" />
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/title_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>